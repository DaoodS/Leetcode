def soll(targ, arr):

    check = [[-1]*len(arr), [-1]*len(arr[0])]

    def checkfnc(m, n):
        if overlap(m, n, targ)!=-1:
            if row:
                m += diff
            else:
                n += diff

            checkfnc(m, n)
        checkfnc(m+1, n)

    def overlap():
        if
        elif:
            
        else:
            return -1
        return flag (row or col), diff

    checkfnc(0, 0)
